{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "$id": "Sanoid.dataset.schema.json",
  "type": "object",
  "description": "Configuration schema for Sanoid.net dataset configuration items",
  "properties": {
    "$comments": {
      "type": "string",
      "description": "Comments about this item."
    },
    "Template": {
      "type": "string",
      "description": "The name of a configured template to use.",
      "default": "default" 
    },
    "Enabled": {
      "type": "boolean",
      "description": "Whether this dataset will be processed (true - default) or not (false). Affects both new snapshots and pruning of expired snapshots.",
      "default": true 
    },
    "TemplateOverrides": {
      "$ref": "Sanoid.template.schema.json",
      "description": "Overrides to apply to this dataset." 
    } 
  }
}